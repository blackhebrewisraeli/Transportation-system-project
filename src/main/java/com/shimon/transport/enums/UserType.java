package com.shimon.transport.enums;

/**
 * Defines the type of user in the system.
 * Determines whether the user is a regular employee,
 * an external volunteer, a retiree, or administrative staff.
 */
public enum UserType {
    EMPLOYEE,
    VOLUNTEER,
    RETIREE,
    STAFF
}
