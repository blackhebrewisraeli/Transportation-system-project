package com.shimon.transport.enums;

/**
 * Direction of a transport request.
 * PICKUP = home to work, DROPOFF = work to home, BOTH = round trip.
 */
public enum RequestDirection {
    PICKUP,
    DROPOFF,
    BOTH
}
